# 12 表版验证说明

## 已验证

- 在隔离 MySQL 8.4.7 实例完整执行 all.sql，未启动或改动电脑原有 MySQL80 服务。
- 12 张表、5 种角色、100 道启用演示题；已完成答卷包含 20 条明细，总分 100，与答卷分数一致。
- 错误考试题目归属、退款样例不一致、培训时间冲突查询均返回 0。
- 外键、重复报名、错误时间段、超额学时、重复考试、重复试题、错误选项、超额退款共 8 项非法写入均被 MySQL 拒绝。
- 演示密码 Demo@123 与数据库 BCrypt 哈希匹配。
- E-R 图中的实体覆盖全部 12 表；所画关系逐一匹配 SQL 外键，基数按可空性和唯一约束生成，连线无交叉或穿过无关节点、菱形和标签。
- Word 的 12 条关系模式与 SQL 字段一致，21 项用例各一句话；嵌入的原生 OLE 文件内容与本包 all.sql 字节一致。
- 文档渲染为 8 页后逐页检查，关系模式逐条完整换页，图内文字和附件图标可见；Word 自动化未能启动，页面检查使用本地 LibreOffice PDF 渲染，不保存回写原 DOCX。

## 后续还要验证

这些检查不代替实际应用测试。菜单权限、同一时段抢占、跨预约资源冲突、题目锁定、报名实缴与退款上限、抽题归属、超时交卷等规则，由后续 Service 实现并进行接口及并发测试。

本版是独立新库，不提供旧 22 表业务数据自动迁移；原始文件留有备份，旧版仓库内容可通过 Git 历史恢复。
