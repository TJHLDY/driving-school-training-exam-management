# 协作提交说明

1. 从 `main` 拉取最新代码后建立个人分支，例如 `chen-mapper-page`、`liu-service-controller`。
2. 每次提交只包含一个清楚的改动，并写明做了什么，例如 `feat: add enrollment mapper`。
3. 推送后发起 Pull Request，由至少一名组员查看后再合并。
4. 改数据库时同时提交增量 SQL，并更新相关设计说明；不要靠本地删库解决版本问题。
5. 合并前至少说明本地如何启动、测试了什么，以及未完成的内容。
